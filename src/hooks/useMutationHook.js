async function useMutationHook(params) {
    // const { mutate, isPending, isError, error } = useMutation({
    //     mutationFn: ({ usernameOrEmail, password }) => loginUser({ usernameOrEmail, password }),
    //     onSuccess: () => {
    //       console.log("login success");
    //       navigate('/');
    //     },
    //   });
}

